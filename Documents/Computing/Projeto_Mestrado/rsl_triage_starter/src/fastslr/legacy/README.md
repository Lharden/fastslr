# Legacy Snapshot

This folder stores the frozen legacy implementation (`v1.0.x`) that was built
for the original 3-theme workflow (`T1A`, `T1B`, `T1C`).

- No new features should be added here.
- The active universal engine lives in `src/rsl_triage/`.
- Legacy files are preserved for reproducibility and migration support.
